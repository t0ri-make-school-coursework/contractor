<div class="card">
  <div class="card">
    <h4 class="card-title">{{this.title}}</h4>
    <p class="card-text"><strong>{{this.user}}</strong></p>
    <p class="card-text">{{this.content}}</p>
    <!-- Delete link -->
    <p><form method="POST" action="/projects/comments/{{this._id}}?_method=DELETE">
        <button class="btn btn-link" type="submit">Delete</button>
    </form></p>
  </div>
</div>
